// duck pattern
// Action value

// Action creator

// 초기 상태값
const initialState = {};

// 리듀서
const example = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default example;
